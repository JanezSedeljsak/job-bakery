<%= form_with(model: applicant, local: true) do |form| %>
  <% if applicant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(applicant.errors.count, "error") %> prohibited this applicant from being saved:</h2>

      <ul>
      <% applicant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :User_id %>
    <%= form.text_field :User_id, id: :applicant_User_id %>
  </div>

  <div class="field">
    <%= form.label :Job_id %>
    <%= form.text_field :Job_id, id: :applicant_Job_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
