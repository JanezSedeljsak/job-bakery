          <div class="col-sm">
            <div class="btn-group dropleft">
                <button id="area_" style="border-bottom: 3px solid #0099cc; width: 10vw" type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Job Title
                </button>
                <div class="dropdown-menu">
                <% @areas.each do |area| %>
                  <a class="dropdown-item" onclick="window.event.preventDefault(); document.getElementById('area_').innerHTML = '<%= area.title %>'" href="#"><%= area.title %></a>
                <% end %>
                </div>
            </div>
          </div>
          <div class="col-sm">
            <div class="btn-group dropleft">
                <button id="loaction_" style="border-bottom: 3px solid #0099cc; width: 10vw" type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Location
                </button>
                <div class="dropdown-menu">
                <% @locations.each do |location| %>
                  <a class="dropdown-item" onclick="window.event.preventDefault(); document.getElementById('loaction_').innerHTML = '<%= location.title %>'" href="#"><%= location.title %>&nbsp;|&nbsp;<%= location.postcode %></a>
                <% end %>
                </div>
            </div>